# Code Review: All-In-One Dashboard and Ticketing Build

Review ID: 2026-06-28-193203-code-review
Date: 2026-06-28
Git branch: Unknown / not a git checkout
Git commit: Unknown / not a git checkout
Status: Passed

## Files Reviewed

- `index.js`
- `src/main.js`
- `src/bot/*`
- `src/db/index.js`
- `src/services/*`
- `src/web/*`
- `public/*`
- `tests/*`
- `package.json`
- `README.md`
- `CHANGELOG.md`
- `CODEMAP.md`
- `VERSION.json`
- `.github/workflows/ci.yml`

## Summary

The single-file purge bot was replaced with a modular Discord bot, PostgreSQL schema migration, dashboard API, static dashboard UI, live feed, scheduler, moderation services, automod, custom commands, welcome/autoroles, leveling/economy stubs, and ticketing controls.

Ticketing includes panel creation, private ticket channels, staff role access, claim, close, and transcript persistence.

## Correctness Review

- JavaScript parse checks passed for all `.js` files.
- Shared pure tests passed for template rendering, live feed behavior, and media matching.
- Full command registration tests passed after dependencies were installed.
- Runtime bot/dashboard startup still requires real Discord and Railway environment variables.

## Security Review

- Dashboard auth uses Discord OAuth and HTTP-only signed cookies.
- Dashboard guild access is filtered to owner, Administrator, or Manage Server guilds.
- Custom command templates suppress `@everyone` and `@here` by default.
- Moderation actions check Discord role hierarchy and bot permissions when operating from Discord interactions.

## Dependency Review

- Added `express` and `cookie-parser` for dashboard/control panel support.
- Existing `discord.js`, `dotenv`, and `pg` remain required.
- Dependency installation succeeded with `NODE_OPTIONS=--use-system-ca`.
- `package-lock.json` was produced and included in the working tree.

## Test Coverage Review

- Covered: template rendering, mass-mention suppression, live feed publish/history, media-family matching.
- Covered: command registration data, template rendering, mass-mention suppression, live feed publish/history, media-family matching.
- Not covered yet: dashboard OAuth, PostgreSQL migrations, live Discord ticket channel permission overwrites, scheduler jobs, dashboard moderation actions.

## Documentation Review

- Updated README, changelog, version metadata, code map, env example, and CI workflow.
- Added this code review report.

## Risks

- Full runtime behavior needs validation with live Railway variables and a Discord test server.
- Ticket transcripts currently store only the most recent 100 messages.
- Dashboard is functional but intentionally basic; advanced ticket forms and transcript export formats are future work.
- The local folder is not a git checkout, so branch/commit lifecycle fields are unknown.

## Required Follow-Up

1. Configure Discord OAuth callback to `/auth/callback`.
2. Run a live Railway smoke test.
3. Verify ticket panel open/claim/close flow in a Discord test server.
